Given 4 number, each one should be separated - as in 4 3 2 1 or 12 4 2 144 -, get the operations to apply to each one
to get some number. The operations are: add, substract, multiplication, division (except for the division by 0). The order of number couldn't be change. But there could be parenthesis between each operation

Given the decider, that given some combinations of operations over the number tries the example and returns the result

Generates some way to generate all posible cases for the graph of operaitons